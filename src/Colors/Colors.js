import { Dimensions } from "react-native"
const {width,height}=Dimensions.get('window')
export const COLORS = {
    white: '#fff',
    black: '#000',
    main:'#176e3e',
  }

  export const Sizes = {
    width,
    height,
    h1:22,
  }